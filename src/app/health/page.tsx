"use client";

import { FormEvent, useEffect, useState } from "react";
import { HeartPulse, Save } from "lucide-react";
import { AppSidebar } from "@/components/layout/app-sidebar";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
type Labs = { alt: string; ast: string; ggt: string; ldl: string; hdl: string; triglycerides: string; glucose: string; date: string };
const initial: Labs = { alt: "74", ast: "64", ggt: "31", ldl: "168", hdl: "61", triglycerides: "87", glucose: "88", date: "2026-07-24" };
const fields: { key: keyof Omit<Labs, "date">; label: string; unit: string; goal: string }[] = [{ key: "alt", label: "ALT / GPT", unit: "U/L", goal: "Objetivo: < 40" }, { key: "ast", label: "AST / GOT", unit: "U/L", goal: "Objetivo: < 40" }, { key: "ggt", label: "GGT", unit: "U/L", goal: "Según laboratorio" }, { key: "ldl", label: "LDL", unit: "mg/dL", goal: "Objetivo: < 130" }, { key: "hdl", label: "HDL", unit: "mg/dL", goal: "Deseable: > 40" }, { key: "triglycerides", label: "Triglicéridos", unit: "mg/dL", goal: "Objetivo: < 150" }, { key: "glucose", label: "Glucosa", unit: "mg/dL", goal: "Objetivo: 70–99" }];
export default function HealthPage() { const [labs, setLabs] = useState<Labs>(initial); const [history, setHistory] = useState<Labs[]>([]); const [saved, setSaved] = useState(false); useEffect(() => { const existing = localStorage.getItem("athlos-latest-labs"); const savedHistory = localStorage.getItem("athlos-labs"); if (existing) setLabs(JSON.parse(existing)); if (savedHistory) setHistory(JSON.parse(savedHistory)); }, []); function submit(event: FormEvent) { event.preventDefault(); const next = [...history.filter((item) => item.date !== labs.date), labs].sort((a, b) => a.date.localeCompare(b.date)); setHistory(next); localStorage.setItem("athlos-latest-labs", JSON.stringify(labs)); localStorage.setItem("athlos-labs", JSON.stringify(next)); setSaved(true); setTimeout(() => setSaved(false), 3000); } return <div className="min-h-screen bg-slate-50"><AppSidebar /><main className="lg:pl-72"><div className="mx-auto max-w-5xl px-4 py-7 sm:px-8 lg:px-10"><header className="mb-8"><p className="mb-2 text-sm font-semibold text-emerald-700">SALUD</p><h1 className="text-3xl font-bold tracking-tight text-slate-950">Salud hepática y analíticas</h1><p className="mt-2 text-slate-600">Registra resultados para comparar la evolución con el peso, el alcohol y el entrenamiento.</p></header><Card className="mb-6 border-amber-200 bg-amber-50 shadow-none"><CardContent className="flex gap-3 p-5"><HeartPulse className="mt-0.5 size-5 shrink-0 text-amber-700"/><p className="text-sm leading-6 text-amber-950">Esta sección es un registro y no sustituye a tu médico. Consulta cualquier resultado o síntoma con el profesional que te atiende.</p></CardContent></Card><form onSubmit={submit}><Card className="border-slate-200 shadow-sm"><CardHeader><CardTitle>Analítica</CardTitle><input aria-label="Fecha de analítica" type="date" value={labs.date} onChange={(event) => setLabs({ ...labs, date: event.target.value })} className="mt-4 rounded-lg border border-slate-200 px-3 py-2 text-sm"/></CardHeader><CardContent className="grid gap-5 p-5 pt-0 sm:grid-cols-2 lg:grid-cols-3">{fields.map(({ key, label, unit, goal }) => <label key={key}><span className="text-sm font-semibold text-slate-800">{label}</span><span className="mt-1 block text-xs text-slate-500">{goal}</span><span className="relative mt-3 block"><input required type="number" min="0" step="any" value={labs[key]} onChange={(event) => setLabs({ ...labs, [key]: event.target.value })} className="w-full rounded-xl border border-slate-200 px-3 py-3 outline-none focus:border-emerald-600 focus:ring-4 focus:ring-emerald-100"/><span className="pointer-events-none absolute right-3 top-3 text-xs text-slate-400">{unit}</span></span></label>)}</CardContent></Card><div className="mt-6 flex items-center gap-4"><button className="inline-flex items-center gap-2 rounded-xl bg-emerald-700 px-5 py-3 text-sm font-semibold text-white hover:bg-emerald-800"><Save className="size-4"/>Guardar analítica</button>{saved && <span className="text-sm font-medium text-emerald-700">Analítica guardada</span>}</div></form>{history.length > 0 && <Card className="mt-8 border-slate-200 shadow-sm"><CardHeader><CardTitle>Historial</CardTitle></CardHeader><CardContent className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b text-xs uppercase tracking-wide text-slate-500"><tr><th className="pb-3">Fecha</th><th className="pb-3">ALT</th><th className="pb-3">AST</th><th className="pb-3">GGT</th><th className="pb-3">LDL</th><th className="pb-3">Glucosa</th></tr></thead><tbody>{[...history].reverse().map((item) => <tr key={item.date} className="border-b border-slate-100 last:border-0"><td className="py-4">{new Date(item.date).toLocaleDateString("es-ES")}</td><td className="py-4 font-semibold">{item.alt}</td><td className="py-4">{item.ast}</td><td className="py-4">{item.ggt}</td><td className="py-4">{item.ldl}</td><td className="py-4">{item.glucose}</td></tr>)}</tbody></table></CardContent></Card>}</div></main></div>; }
