"use client";

import { FormEvent, ChangeEvent, useEffect, useRef, useState } from "react";
import { Download, Save, SlidersHorizontal, Upload } from "lucide-react";
import { AppSidebar } from "@/components/layout/app-sidebar";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

type Goals = { targetWeight: string; targetWaist: string; targetSteps: string; targetStrength: string; targetAlcohol: string };
const initial: Goals = { targetWeight: "87", targetWaist: "95", targetSteps: "8000", targetStrength: "3", targetAlcohol: "0" };
const fields: { key: keyof Goals; label: string; detail: string; unit: string }[] = [{ key: "targetWeight", label: "Peso objetivo", detail: "Primer objetivo realista de salud", unit: "kg" }, { key: "targetWaist", label: "Cintura objetivo", detail: "Medida a la altura del ombligo", unit: "cm" }, { key: "targetSteps", label: "Pasos diarios", detail: "Media diaria semanal", unit: "pasos" }, { key: "targetStrength", label: "Sesiones de fuerza", detail: "Objetivo cada semana", unit: "sesiones" }, { key: "targetAlcohol", label: "Alcohol semanal", detail: "Objetivo con alcohol; 0,0 no cuenta", unit: "ml" }];
export default function SettingsPage() { const [goals, setGoals] = useState<Goals>(initial); const [saved, setSaved] = useState(false); const [imported, setImported] = useState(false); const [importError, setImportError] = useState(""); const fileRef = useRef<HTMLInputElement>(null); useEffect(() => { const stored = localStorage.getItem("athlos-goals"); if (stored) setGoals(JSON.parse(stored)); }, []); function submit(event: FormEvent) { event.preventDefault(); localStorage.setItem("athlos-goals", JSON.stringify(goals)); setSaved(true); setTimeout(() => setSaved(false), 3000); } function exportData() { const backup = { exportedAt: new Date().toISOString(), goals: JSON.parse(localStorage.getItem("athlos-goals") ?? "{}"), checkIns: JSON.parse(localStorage.getItem("athlos-checkins") ?? "[]"), labs: JSON.parse(localStorage.getItem("athlos-labs") ?? "[]") }; const url = URL.createObjectURL(new Blob([JSON.stringify(backup, null, 2)], { type: "application/json" })); const link = document.createElement("a"); link.href = url; link.download = `athlos-respaldo-${new Date().toISOString().slice(0, 10)}.json`; link.click(); URL.revokeObjectURL(url); } function importData(event: ChangeEvent<HTMLInputElement>) { const file = event.target.files?.[0]; if (!file) return; setImportError(""); const reader = new FileReader(); reader.onload = (e) => { try { const data = JSON.parse(e.target?.result as string); if (!data.checkIns && !data.labs && !data.goals) { setImportError("El archivo no tiene el formato de respaldo de Athlos."); return; } if (data.goals && Object.keys(data.goals).length) localStorage.setItem("athlos-goals", JSON.stringify(data.goals)); if (Array.isArray(data.checkIns)) localStorage.setItem("athlos-checkins", JSON.stringify(data.checkIns)); if (Array.isArray(data.labs)) localStorage.setItem("athlos-labs", JSON.stringify(data.labs)); setImported(true); setTimeout(() => setImported(false), 4000); window.location.reload(); } catch { setImportError("No se pudo leer el archivo. Asegúrate de que es un JSON válido."); } }; reader.readAsText(file); if (fileRef.current) fileRef.current.value = ""; } return <div className="min-h-screen bg-slate-50"><AppSidebar /><main className="lg:pl-72"><div className="mx-auto max-w-4xl px-4 py-7 sm:px-8 lg:px-10"><header className="mb-8"><p className="mb-2 text-sm font-semibold text-emerald-700">CONFIGURACIÓN</p><h1 className="text-3xl font-bold tracking-tight text-slate-950">Tus objetivos</h1><p className="mt-2 text-slate-600">Define las referencias que usarán el dashboard y los semáforos. Podemos ajustarlas sin perder tu historial.</p></header><form onSubmit={submit}><Card className="border-slate-200 shadow-sm"><CardHeader><CardTitle className="flex items-center gap-2"><SlidersHorizontal className="size-5 text-emerald-700"/>Objetivos del proyecto</CardTitle></CardHeader><CardContent className="grid gap-6 p-5 pt-0 sm:grid-cols-2">{fields.map(({ key, label, detail, unit }) => <label key={key}><span className="text-sm font-semibold text-slate-800">{label}</span><span className="mt-1 block text-xs text-slate-500">{detail}</span><span className="relative mt-3 block"><input required type="number" min="0" step="any" value={goals[key]} onChange={(event) => setGoals({ ...goals, [key]: event.target.value })} className="w-full rounded-xl border border-slate-200 px-3 py-3 outline-none focus:border-emerald-600 focus:ring-4 focus:ring-emerald-100"/><span className="pointer-events-none absolute right-3 top-3 text-sm text-slate-400">{unit}</span></span></label>)}</CardContent></Card><div className="mt-6 flex flex-wrap items-center gap-4"><button className="inline-flex items-center gap-2 rounded-xl bg-emerald-700 px-5 py-3 text-sm font-semibold text-white hover:bg-emerald-800"><Save className="size-4"/>Guardar objetivos</button>{saved && <span className="text-sm font-medium text-emerald-700">Objetivos guardados</span>}</div></form><Card className="mt-8 border-slate-200 shadow-sm"><CardHeader><CardTitle>Copia de seguridad</CardTitle></CardHeader><CardContent className="space-y-4 p-5 pt-0"><div className="flex flex-col items-start gap-4 sm:flex-row sm:items-center sm:justify-between"><p className="max-w-xl text-sm leading-6 text-slate-600">Descarga tus objetivos, check-ins y analíticas en un único archivo. Las fotografías permanecen privadas en el navegador y no forman parte de esta copia.</p><button type="button" onClick={exportData} className="inline-flex shrink-0 items-center gap-2 rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm font-semibold text-slate-700 hover:bg-slate-50"><Download className="size-4"/>Descargar copia</button></div><hr className="border-slate-200"/><div className="flex flex-col items-start gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-semibold text-slate-800">Restaurar datos</p><p className="text-xs text-slate-500">Selecciona un archivo de respaldo para restaurar objetivos, check-ins y analíticas en este dispositivo.</p></div><input ref={fileRef} type="file" accept=".json" onChange={importData} className="hidden"/><button type="button" onClick={() => fileRef.current?.click()} className="inline-flex shrink-0 items-center gap-2 rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm font-semibold text-slate-700 hover:bg-slate-50"><Upload className="size-4"/>Importar respaldo</button></div>{importError && <p className="text-sm font-medium text-rose-600">{importError}</p>}{imported && <p className="text-sm font-medium text-emerald-700">Datos restaurados correctamente. Recargando...</p>}</CardContent></Card></div></main></div>; }
