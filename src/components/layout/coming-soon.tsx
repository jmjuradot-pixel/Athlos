import { AppSidebar } from "@/components/layout/app-sidebar";

export function ComingSoon({ eyebrow, title, description }: { eyebrow: string; title: string; description: string }) { return <div className="min-h-screen bg-slate-50"><AppSidebar /><main className="lg:pl-72"><div className="mx-auto max-w-4xl px-4 py-7 sm:px-8 lg:px-10"><p className="mb-2 text-sm font-semibold text-emerald-700">{eyebrow}</p><h1 className="text-3xl font-bold tracking-tight text-slate-950">{title}</h1><p className="mt-2 max-w-2xl text-slate-600">{description}</p><div className="mt-8 rounded-2xl border border-dashed border-slate-300 bg-white p-8 text-center"><p className="font-semibold text-slate-800">Este módulo será el siguiente en conectarse a tus datos.</p><p className="mt-2 text-sm text-slate-500">La estructura ya está lista para que no tengamos que rehacer la navegación más adelante.</p></div></div></main></div>; }
